SPECIFICATION Spec

\* Terminal states (every ticket merged or failed) are legitimate end states,
\* not deadlocks — liveness is checked dynamically by the Go suite (Settled).
CHECK_DEADLOCK FALSE

INVARIANT TypeOK
INVARIANT MergeImpliesVerified
INVARIANT MergedAtMostOnce
INVARIANT NoDuplicateMergedKey
INVARIANT ApprovalGate
